
services:
  web:
    build: .
    tty: true
    ports:
      - "8003:8000"
    env_file:
      .env
    depends_on:
      # - db
      - redis
    volumes:
      - ./src:/aviacassa
    
  # db:
  #   image: postgres:16
  #   env_file:
  #     - .env
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - 5437:5432

  redis:
    image: redis:7

# volumes:
#   pgdata: